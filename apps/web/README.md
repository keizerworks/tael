# @tael/web

Reserved for the Tael web dashboard (hosted sync, team memory).

> **Not started yet — intentionally.** Per the v0.1 plan, all early work lives in
> `packages/cli` and `packages/core`. The web app lands in v0.4. This directory
> exists only to lock in the monorepo layout.

See [`docs/roadmap.md`](../../docs/roadmap.md).
